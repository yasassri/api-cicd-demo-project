environments:
  - name: dev
    endpoints:
      production:
      sandbox:
  - name: qa
    endpoints:
      production:
      sandbox:

deploy:
  import:
    update: true
    preserveProvider: true
